# Tests que FABRICAN un artefacto que existe de verdad en `data/reference/`.
#
# La lista la deriva `tools/auditar_fixtures.py`; aquí sólo vive lo que no se puede
# derivar: POR QUÉ ese test no usa el real.
#
# Fabricar no está prohibido —probar el caso malo, la cabecera corta, un md5 que no
# cuadra o un fichero de 5,6 MB que no hace falta son motivos buenos—. Lo que no vale es
# no decirlo: un fixture sintético donde existe el real valida un camino que nadie
# recorre, que es la segunda mitad del principio nº 18.
#
# `usa_el_real = true` es para el caso en que alguien cablee el real y la entrada se
# quede: entonces la justificación caduca y el test lo dice.

[[fixture]]
test = "test_directorio_trabajo.py"
artefacto = "NM_011170.3.fa"
usa_el_real = false
por_que_no_el_real = "La siembra tiene que comprobar que NO PISA lo ya subido, y para eso el fichero de destino tiene que ser DISTINTO del versionado: se escribe «MIO» y se exige que siga ahí después de sembrar. Con el real, un fichero idéntico no distinguiría «no lo pisó» de «lo pisó con lo mismo»."

[[fixture]]
test = "test_gestor_referencias.py"
artefacto = "mature.fa"
usa_el_real = false
por_que_no_el_real = "El real son 5,6 MB y aquí sólo se prueban las CUATRO ACCIONES del gestor —ver, reemplazar, borrar, descargar—, que no miran el contenido. Copiarlo en cada uno de los tests de este fichero multiplicaría el tiempo de la suite sin comprobar nada más."

[[fixture]]
test = "test_gestor_referencias.py"
artefacto = "rmsk_mouse.out"
usa_el_real = false
por_que_no_el_real = "Igual: lo que se prueba es el plan de reemplazo y qué corridas invalida, no el parseo del `.out` —que tiene sus propios tests con el fichero real."

[[fixture]]
test = "test_manifiesto.py"
artefacto = "NM_011170.3.fa"
usa_el_real = false
por_que_no_el_real = "Se prueba el ESCRITOR del manifiesto sobre un directorio controlado: hacen falta ficheros cuyos md5 y tamaños se conozcan de antemano para poder afirmar qué línea sale. Con los reales el test comprobaría el manifiesto real, que ya tiene su propio test."

[[fixture]]
test = "test_manifiesto.py"
artefacto = "PROCEDENCIA.md"
usa_el_real = false
por_que_no_el_real = "Es un fichero de una línea que sólo existe para comprobar que NO sale en `unlisted`. Su contenido real no interviene."

[[fixture]]
test = "test_manifiesto.py"
artefacto = "mature.fa"
usa_el_real = false
por_que_no_el_real = "Lo mismo: md5 y tamaño conocidos para poder afirmar la línea que escribe el manifiesto."

[[fixture]]
test = "test_mirna.py"
artefacto = "mature.fa"
usa_el_real = false
por_que_no_el_real = "Se prueba el PARSER con casos concretos —alfabeto ARN y ADN, prefijos, un maduro corto— que en el fichero real habría que ir a buscar y que podrían desaparecer en el siguiente release de miRBase. Las cifras del fichero real se fijan aparte, en `test_abundancia_dos_capas.py`."

[[fixture]]
test = "test_obsoleto.py"
artefacto = "mature.fa"
usa_el_real = false
por_que_no_el_real = "Sólo hace falta un md5 distinto del registrado para que una corrida quede obsoleta. El contenido no interviene: lo que se prueba es la comparación de checksums."

[[fixture]]
test = "test_polyadb_es_un_fichero.py"
artefacto = "polya_db_mouse.tsv"
usa_el_real = false
por_que_no_el_real = "Se prueban los RECHAZOS del parser —columna que falta, PSE fuera de rango, fichero vacío—, y para eso hace falta un fichero MAL. El bueno se carga del real en el mismo módulo, que es lo que fija las cifras."

[[fixture]]
test = "test_recursos.py"
artefacto = "aav_casete.fa"
usa_el_real = false
por_que_no_el_real = "El casete real son 5282 pb y aquí se prueba el CABLEADO rol→cargador, no la secuencia. Se usa un casete corto declarado en el propio test."

[[fixture]]
test = "test_roles_manifiesto.py"
artefacto = "mature.fa"
usa_el_real = false
por_que_no_el_real = "Se prueba que el rol conecta con su cargador; el contenido sólo tiene que ser un FASTA válido."

[[fixture]]
test = "test_usar_manifiesto.py"
artefacto = "aav_casete.fa"
usa_el_real = false
por_que_no_el_real = "MOTIVO DE PESO, y es el que enseñó la errata nº 33: este fichero monta manifiestos PARCIALES a propósito, para probar qué pasa cuando falta un rol, cuando un md5 no cuadra, o cuando una bandera explícita manda sobre el manifiesto. Con el real no se pueden montar esos casos. LO QUE FALTABA no era dejar de fabricarlos: era que ADEMÁS hubiera una corrida contra el manifiesto REAL — y esa está ahora en `test_roles_del_manifiesto.py`, donde se vio el `KeyError: polyadb`."

[[fixture]]
test = "test_auditorias_no_se_pisan.py"
artefacto = "aav_casete.fa"
usa_el_real = false
por_que_no_el_real = "NO fabrica nada: el nombre está dentro de un FUENTE SINTÉTICO que se le da al reconocedor de `auditar_fixtures` para comprobar que muerde igual con el nombre literal y con el derivado. El material de prueba de un detector tiene que contener justo lo que el detector busca — es el mismo caso que el fuente de antes del arreglo con el que se prueba `auditar_condiciones`. Y que ESTA entrada exista es además la demostración del principio nº 26: el cruce entre las dos auditorías lo detecta la otra auditoría, así que las dos siguen mirando el mismo hecho."

[[fixture]]
test = "test_la_procedencia_SE_DECLARA_sin_resubir.py"
artefacto = "aav_casete.fa"
usa_el_real = false
por_que_no_el_real = "Aquí el casete no es un casete: es el NOMBRE de un rol que NO pide procedencia de tabla, y lo que se comprueba es que declarársela ABORTE — «un casete de AAV no sale de ninguna tabla, así que ahí la columna vacía es la verdad». `declare_provenance` no abre el fichero: rechaza por el rol antes de mirar nada, así que copiar los 5,3 kB del real no probaría ni una línea más. Lo que sí es del real es el rol, que sale de `species.required_files`."

[[fixture]]
test = "test_un_fichero_SIN_PROCEDENCIA_no_esta_cerrado.py"
artefacto = "manifest.tsv"
usa_el_real = false
por_que_no_el_real = """\
El caso que este test reproduce NO EXISTE en el manifiesto real: es una linea de \
`transcriptoma_3utr.fa` SIN los cuatro campos de procedencia de la tabla — un fichero \
que entro antes de que su frente los exigiera. El manifiesto versionado los tiene \
todos, asi que usarlo no probaria nada; y el fichero real son decenas de megas que no \
se versionan. Lo que se fabrica es la LINEA, con las columnas que declara \
`manifest.MANIFEST_COLUMNS` —derivadas, no tecleadas—, y el test comprueba las dos \
caras: sin los cuatro campos la fila NO sale CERRADA, y declarandolos vuelve a estarlo."""

[[fixture]]
test = "test_la_CAJA_de_procedencia_esta_donde_ABORTA.py"
artefacto = "manifest.tsv"
usa_el_real = false
por_que_no_el_real = """\
El mismo caso que `test_un_fichero_SIN_PROCEDENCIA_no_esta_cerrado.py` y por el mismo \
motivo, aplicado a la CAJA que lo arregla: el estado que se prueba es el fichero DENTRO \
del deposito con su linea SIN los cuatro campos de tabla, y eso no existe en el \
manifiesto versionado —que los tiene todos— ni puede existir, porque el fichero real \
son 84 MB que no van a git. USAR EL DEPOSITO REAL ES LO QUE HIZO FALLAR ESTE TEST LA \
PRIMERA VEZ (errata nº 123): ahi el transcriptoma NO esta, asi que no falta ninguna \
procedencia, la caja ni se pinta y la rama que revienta no se ejecuta — el test pasaba \
recorriendo el camino feliz mientras el modal se caia en produccion. Lo que se fabrica \
es la LINEA, con las columnas derivadas de `manifest.MANIFEST_COLUMNS`."""

# ─────────────────────────────────────────────────────────────────────────────────────
# LAS SEIS QUE EL AUDITOR NO VEIA (2026-09-11). No son fabricaciones nuevas: llevaban
# tiempo ahi y `artefactos_reales()` derivaba del DIRECTORIO, asi que en una maquina sin
# `refseq_rna.fa` ni `transcriptoma_3utr.fa` bajados —o sea en cualquier clon limpio: no
# se versionan— el auditor no los reconocia como artefactos y no pedia justificacion de
# nada. El fallo hacia el silencio que el propio modulo declara. Ahora la lista sale del
# directorio Y del manifiesto versionado, que contesta lo mismo en cualquier maquina.

[[fixture]]
test = "test_el_deposito_se_lee_de_UN_SOLO_sitio.py"
artefacto = "transcriptoma_3utr.fa"
usa_el_real = false
por_que_no_el_real = "Lo que se prueba es que el md5 del disco y el de la fila del manifiesto se comparen: hace falta poder CAMBIAR el fichero debajo de su linea, y con el real —cientos de MB que ademas no estan en el repositorio— no hay forma de montar ese caso. Se usa el FASTA minimo de `test_el_transcriptoma_ENTRA.py`, que ya tiene la forma que el cargador exige."

[[fixture]]
test = "test_el_transcriptoma_ENTRA.py"
artefacto = "transcriptoma_3utr.fa"
usa_el_real = false
por_que_no_el_real = "El real son cientos de MB del Table Browser de UCSC y NO se versiona, asi que ningun test podria usarlo. Lo que se prueba aqui es la VALIDACION al subir —que sea FASTA, el alfabeto, la auditoria de isoformas— y para eso hace falta justo lo contrario del real: ficheros pequeños con identificadores repetidos y secuencias identicas a proposito, que en el real habria que ir a buscar."

[[fixture]]
test = "test_la_corrida_se_reevalua.py"
artefacto = "refseq_rna.fa"
usa_el_real = false
por_que_no_el_real = "El caso es que el fichero APAREZCA, CAMBIE y DESAPAREZCA entre dos consultas, y que la corrida pase de NOT_RUN a PASS y a OBSOLETA sola. Eso exige escribirlo y borrarlo dentro del test; con el real —cientos de MB y fuera del repositorio— la mitad adversaria, la del md5 que ya no cuadra, no se podria montar."

[[fixture]]
test = "test_la_procedencia_SE_DECLARA_sin_resubir.py"
artefacto = "transcriptoma_3utr.fa"
usa_el_real = false
por_que_no_el_real = "Se declara la procedencia sobre la fila que YA esta, sin volver a subir el fichero, y el guardia es que el md5 del disco siga siendo el de su linea. Para probar que aborta hay que cambiar el fichero debajo, o sea escribirlo. El contenido no interviene: lo que se compara son checksums."

[[fixture]]
test = "test_roles_manifiesto.py"
artefacto = "refseq_rna.fa"
usa_el_real = false
por_que_no_el_real = "Se prueba `roles_available` sobre un manifiesto de CABECERA CORTA con md5 conocidos de antemano: hace falta un fichero cuyo md5 cuadre con la fila y otro cuyo md5 NO cuadre, y los dos se escriben con seis bytes. Con el real no se puede montar el segundo, que es el que demuestra que la comprobacion muerde."

[[fixture]]
test = "test_una_base_ENORME_no_entra_al_escaner.py"
artefacto = "refseq_rna.fa"
usa_el_real = false
por_que_no_el_real = "Lo que decide el presupuesto del escaner es `st_size`, y el guardia aborta ANTES de parsear nada, asi que el fichero se crea vacio con `truncate` al tamaño que se quiere medir. No se inventa ninguna secuencia: se inventa un TAMAÑO, que es la magnitud bajo prueba. Fabricar 400 MB de FASTA real seria absurdo y no comprobaria nada mas."
