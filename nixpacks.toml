[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake", "libreoffice"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install 'Pillow>=10.0.0' 'pypdf>=4.0.0' 'pdf2docx>=0.5.8' 'svglib>=1.5.1' 'reportlab>=4.0.0' 2>/dev/null || true"
]
